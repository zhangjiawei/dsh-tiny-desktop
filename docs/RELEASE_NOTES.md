v0.2.6 公开预览版：恢复 Windows 中断安装，并消除系统 Node 探测闪屏。

- 独立 Node 解压后的目录发布新增有界重试，覆盖 Windows Defender、索引器或文件系统短暂返回 `Access is denied` 的情况。
- 检测到上次重启留下的同名半成品 Node 目录时，先移动为应用目录内的可恢复备份；新运行时验证可用后才清理备份。若替换最终失败，会恢复原目录，不触碰 DSH 数据、全局 Node 或全局 DSH。
- Windows 对全局 Node 的 `node --version` 探测与系统语言 PowerShell 探测统一使用隐藏子进程策略，避免双击 GUI EXE 时短暂闪出控制台。正式包继续使用 PE GUI 子系统，长期 DSH/安装子进程也保持隐藏。
- 保留 v0.2.5 的 Windows 默认命令稳定性、真实网卡选择、原生窗口图标和统一退出界面。

升级请先从托盘或设置中真正退出旧版，再替换程序。保留独立数据目录。macOS 仍为 ad-hoc 签名、Windows 未商业签名。

## v0.2.5

v0.2.5 公开预览版：修复 Windows 无控制台首次启动，并统一窗口图标与退出界面。

- Windows GUI 启动的受管 Node/pnpm 子进程使用应用专属的可读 stdin；针对 pnpm 10.28.0 无控制台执行 DSH 极短 postinstall 时仍存在的输出管道竞态，Windows 上精确匹配默认 `pnpm dlx` 命令时复用已经安装并重建好的同版本私有 DSH。真正自定义的命令仍按配置执行。
- 仅复用精确匹配的 Node 24.20.0；其他系统 Node 版本改用校验后的私有固定版本。全局 dsh 不参与启动。
- 设置窗口齿轮图标和工作空间品牌图标直接按系统 DPI 创建原生小/大 HICON；Windows 回归检查实际像素内容，不再只判断句柄存在。
- 侧栏“退出应用”与导航样式统一；确认弹窗改为一致的间距、警示信息层级、按钮尺寸和危险操作配色。
- 局域网地址优先使用默认路由网卡，避免 Windows 误选 WSL/Hyper-V 虚拟地址；自定义命令允许 `--trusted-host` 指定一个私有 IPv4，未指定时自动注入所选局域网 IP。
- 保留 v0.2.4 的旧锁恢复、六个首次安装最新版插件、端口检测和设置保存修复。

升级请先从托盘或设置中真正退出旧版，再替换程序。保留独立数据目录。macOS 仍为 ad-hoc 签名、Windows 未商业签名。

## v0.2.4

v0.2.4 公开预览版：设置窗口、国内仓库默认值、端口提示与可靠的安装恢复。

- 修复安装中断后重试出现 `ERR_PNPM_OUTDATED_LOCKFILE`：仅对应用独立 profile 的安装显式允许更新锁文件，不删除数据、不修改全局 pnpm 配置或全局 DSH。
- 切换仓库不再重装已完成安装的运行环境和六插件，避免无意触发重新初始化/升级。六插件首次安装仍解析 latest。
- v0.2.3 发布任务收到上述用户故障后已取消，未发布安装包；修复和恢复验证一并进入 v0.2.4。

- 修复 Windows 同页 hash 导航期间的桥接请求丢失：只忽略来源 URL 的 fragment 差异，不放宽域名、协议、路径、查询和窗口校验。v0.2.2 发布测试被保存超时拦截，保留原标签但没有发布安装包。

- “控制中心”统一改名“设置”，采用齿轮标识；Windows 设置窗口使用独立齿轮图标，macOS 保留应用共享 Dock 图标。
- 设置侧栏新增“退出应用”，确认后停止后台 DSH 并退出，取消不会中断运行。
- 首次安装默认填入 npmmirror 国内仓库和私有 pnpm dlx 启动命令，支持单项恢复默认；已有自定义值保留，旧版空命令显示为等价的 `dsh web`。
- 输入框明确说明命令限制和仓库优先级。镜像可能有同步延迟；六插件首次安装解析最新版本，普通重启不强制升级。
- 端口占用自动检测；运行成功后突出显示“端口已占用，已使用随机端口 xxxx”。提示跟随正在运行的服务，不受尚待重启的设置影响。
- 新增 Windows 原生回归：真实占用端口、默认命令与镜像、退出取消及确认后释放后台端口。

升级请先从托盘或设置中真正退出旧版，再替换程序。保留独立数据目录。macOS 仍为 ad-hoc 签名、Windows 未商业签名。

## v0.2.1

修复 Windows 控制中心连接与运行中保存设置。

- 修复 Windows 控制中心一直连接、端口/数据目录/日志空白、按钮无响应：按 Wails 各平台实际提供的来源字段校验，不再要求 Windows 上缺失的 IsMainFrame。Linux 同类问题同步修正，并禁止控制页嵌套 frame。
- 保存设置无需停止服务，启动参数保存后在下次启动生效；待应用状态清楚显示。“应用并重启”仍为单独操作。
- 新安装默认开启自动启动、关闭后继续运行、仅驻留托盘和可信私有局域网分享，仅工作空间置顶关闭；已有设置不覆盖，凭据导入仍为主动选择。
- 没有私有 IPv4 时继续本机启动；二维码提示依据实际生效的分享地址，不误用尚未应用的开关。
- 控制通信失败 8 秒后明确提示并重试，不再等待两分钟才报错。
- 新增 Windows x64/ARM64 原生 WebView2 发布门禁，测试真实控制页、DSH 启动、数据/日志、运行中保存、窗口/二维码、停止和下次启动配置。

包含 v0.2.0 能力：

- 可选“最小化/关闭后仅驻留托盘”，点击托盘恢复工作空间；恢复不再重新加载当前对话。
- 默认跟随系统语言，可即时切换简体中文 / English；其他系统语言回退 English。仅切换外壳，不改动 DSH 上游界面。
- 修复切换页面后概览按钮及状态刷新被来源检查误拒绝的问题，保留本地控制页与主框架的权限边界。
- 修复 macOS 托盘点击调用 Dock API 时的主线程同步派发崩溃。
- 新增启动命令、HTTPS registry、启动超时与“应用并重启”，提供私有 pnpm dlx 示例。端口、监听和认证仍由外壳管理。
- 六个预置插件在首次运行安装时自动获取最新版，无需设置。精确版本保存到安装回执，普通重启保持已有版本，不静默升级。
- 控制中心重新设计：白色/浅灰底、绿色强调色、紧凑导航、分组设置、多行启动命令输入；中英文布局适配最小窗口尺寸。
- CI 新增真实 pnpm dlx 启动及其原生 PTY 验证，覆盖六种平台/架构。

保留 v0.1.0 能力：

- 自动准备 Node 24 / pnpm / DSH 与六个预置插件。
- 3080 端口冲突回退、认证就绪检查、按进程树退出。
- 控制中心、脱敏日志、浏览器打开、认证链接复制/二维码。
- 独立数据空间、导入预览、可选凭据复制、备份恢复。
- 原创图标、托盘、刷新/缩放/置顶；不包含分屏。

下载前请阅读 README 与 docs/VALIDATION.md。macOS 为 ad-hoc 签名而非 Apple 公证；Windows 未商业签名。Linux 需要 GTK4/WebKitGTK 6.0。认证链接包含完整权限，不要公开；新安装默认开启可信私有局域网分享（可关闭），HTTP 未加密，系统权限或防火墙可能阻止访问。第三方插件的模型调用、IM 平台绑定、通知能力仍需用户配置相应凭据与系统权限。

构建产物不包含个人配置、密钥或运行时数据。SHA256SUMS.txt 可校验下载完整性；它不是商业发布者签名。
