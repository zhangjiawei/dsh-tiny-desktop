首个公开预览版：Wails v3 原生 WebView + 独立 DSH 进程管理。

- 自动准备 Node 24 / pnpm / DSH 与六个固定版本插件。
- 3080 端口冲突回退、认证就绪检查、按进程树退出。
- 控制中心、脱敏日志、浏览器打开、认证链接复制/二维码。
- 独立数据空间、导入预览、可选凭据复制、备份恢复。
- 原创图标、托盘、刷新/缩放/置顶；不包含分屏。

下载前请阅读 README 与 docs/VALIDATION.md。macOS 为 ad-hoc 签名而非 Apple 公证；Windows 未商业签名。Linux 需要 GTK4/WebKitGTK 6.0。认证链接仅用于本机且包含完整权限，不要公开。第三方插件的模型调用、IM 平台绑定、通知能力仍需用户配置相应凭据与系统权限。

构建产物不包含个人配置、密钥或运行时数据。SHA256SUMS.txt 可校验下载完整性；它不是商业发布者签名。
