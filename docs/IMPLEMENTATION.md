# 实施计划

## 已确认范围

Wails v3 / Go / TypeScript；公开仓库 zhangjiawei/dsh-tiny-desktop；独立数据目录及导入；无分屏。
现有 Node 兼容时复用，否则私有安装 Node 24；私有 pnpm；预装六个指定插件。
默认端口 3080，冲突自动换端口；捕获官方启动 URL、正常 cookie 认证、可随时复制当前链接。
支持托盘、窗口尺寸记忆、置顶、代理、日志、导入文件选择、更新入口和跨平台 Release。
首版不提供跨平台透明度、窗口坐标记忆或静默自动更新；DSH 自身的文件交互沿用上游 Web UI。

## 工作顺序

- [x] Wails v3 原生窗口与外部 DSH 页面验证（macOS Intel GUI）
- [x] 运行环境下载与校验、固定版本安装、失败后重试
- [x] DSH 进程所有权、端口竞争、认证及分享
- [x] 控制面板、原创图标、窗口/托盘与导入文件选择
- [x] 独立数据目录、导入预览/备份/恢复、更新入口
- [x] 六插件真实装配与冷启动验证
- [x] 测试/构建 CI 与公开仓库（六平台原生测试及打包通过）
- [x] 发布 v0.1.0 tag，六平台再次验证并生成 Release 资产及 SHA256SUMS.txt

## v0.2.0 进度

- [x] 四项增量功能实现，来源检查与语言轮询回归先失败后通过
- [x] 六平台真实安装、自定义 pnpm dlx、原生 PTY 和打包门禁通过（`be2798c`）
- [x] 本机候选包更新并启动，保留独立数据及旧包备份
- [x] Mac 解锁后的托盘完整循环及概览按钮原生点击回归
- [x] 首次安装自动解析六插件 latest，移除策略设置；已有回执离线复用回归
- [x] 白灰绿控制中心重设计，中英文和 820×680 最小尺寸验证
- [ ] v0.2.0 tag / Release 工作流、公开资产及校验和复核（用户已授权直接发布，无需重复请求授权）

## 安全约束

v0.2.0 增量：系统语言/中英文切换、托盘驻留开关、控制桥 hash 路由修复、可配置 argv 启动、registry/超时。首次安装自动解析六插件 latest，无策略开关；已有安装复用精确版本回执。原生窗口恢复保持当前页面，不重新认证或刷新对话。控制中心采用白色、浅灰、绿色强调色及分组设置。

不修改系统 PATH、不申请 sudo、不修改既有 DshShell、不直接绑定 DSH 到公网、不关闭认证。
主 DSH 窗口与可信的本地控制页分离；外链交给系统浏览器，不能访问特权 Go 操作。
日志默认脱敏；令牌只在内存保存，分享明确提示其不是只读权限。
依赖构建仅对锁定且审核的必要依赖授权。

## 发布门槛

测试通过不等于原生 GUI 验证通过。每个平台注明构建、冷启动、GUI、签名状态。
签名证书不可用时只能提供明确标注的未签名预发布包。
